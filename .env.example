# Ably — real-time pub/sub for broadcast events
ABLY_API_KEY=

# Upstash Redis — queue, slot state, distributed locks
KV_REST_API_URL=
KV_REST_API_TOKEN=

# JWT — signs per-slot tokens
JWT_SECRET=

# Cron — optional, protects the /api/cron endpoint
CRON_SECRET=
